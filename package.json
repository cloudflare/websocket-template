{
  "private": true,
  "name": "websocket-template",
  "version": "1.0.0",
  "description": "A template for kick starting a Cloudflare Workers project, with websocket server and browser + CLI client included",
  "main": "ws_server_worker.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "Kristian Freeman <kristian@kristianfreeman.com>",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^1.18.2"
  }
}
